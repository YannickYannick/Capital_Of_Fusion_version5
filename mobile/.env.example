# URL du backend Django (données Supabase via ORM Django — pas de connexion Supabase directe).
#
# Téléphone / Expo Go (recommandé) :
EXPO_PUBLIC_API_URL=https://capitaloffusionversion5-production.up.railway.app
#
# Émulateur Android + Django local :
# EXPO_PUBLIC_API_URL=http://10.0.2.2:8000
#
# Téléphone + Django local (même Wi-Fi, ipconfig → IPv4) :
# EXPO_PUBLIC_API_URL=http://192.168.x.x:8000
