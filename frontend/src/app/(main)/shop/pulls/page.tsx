import { SkeletonPage } from "@/components/shared/SkeletonPage";

export default function SubSkeletonPage() {
  return (
    <SkeletonPage 
      title="Shop / Pulls" 
      description="Cette section (Shop / Pulls) sera disponible prochainement en Phase 2." 
    />
  );
}
