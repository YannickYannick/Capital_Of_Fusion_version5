import { SkeletonPage } from "@/components/shared/SkeletonPage";

export default function SubSkeletonPage() {
  return (
    <SkeletonPage 
      title="Care / Reservation" 
      description="Cette section (Care / Reservation) sera disponible prochainement en Phase 2." 
    />
  );
}
