import { SkeletonPage } from "@/components/shared/SkeletonPage";

export default function SubSkeletonPage() {
  return (
    <SkeletonPage 
      title="Organisation / Structure" 
      description="Cette section (Organisation / Structure) sera disponible prochainement en Phase 2." 
    />
  );
}
